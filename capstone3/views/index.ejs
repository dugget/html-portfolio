<%- include('partials/header.ejs') %>

<button id="createPostBtn" class="btn btn-primary createPostBtn">Create Post</button>

<div class="posts">
    <% posts.forEach(post => { %>
        <div class="post">
            <h3><%= post.title %></h3>
            <p><%= post.body.substring(0, 100) %>...</p>
            <p>By <%= post.author %></p>
            <a href="/posts/<%= post.title %>">Read More</a>
            <form action="/delete/<%= post.title %>" method="POST">
                <button type="submit" class="btn btn-danger deletePostBtn">Delete Post</button>
            </form>
            <a href="/posts/edit/<%= post.title %>" class="btn btn-primary editPostBtn">Edit Post</a>
        </div>
    <% }) %>
</div>


<script>
    document.getElementById("createPostBtn").addEventListener("click", () => {
        window.location.href = "/create";
    });
</script>
<%- include("partials/footer.ejs") %>

